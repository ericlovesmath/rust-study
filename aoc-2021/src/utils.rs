// use std::fs;

// pub fn read_data(file: &str) -> Vec<String> {
    // let values = fs::read_to_string(file).expect("Could not load file");
    // values
        // .lines()
        // .map(String::from)
        // .collect::<Vec<String>>()
// }
